DATABASE_URL=postgresql://manolo:manolo@localhost:5432/manolo
OPENAI_API_KEY=sua_chave_openai_aqui
# quando migrar para Claude: adicionar ANTHROPIC_API_KEY e trocar model string
STORAGE_BUCKET=seu_bucket_aqui
WHATSAPP_TOKEN=seu_token_whatsapp_aqui
WHATSAPP_VERIFY_TOKEN=seu_verify_token_aqui

# Web App (Fase 4) — adicionar no Render em produção
# Origens separadas por vírgula (local + Vercel)
WEB_CORS_ORIGINS=http://localhost:3000,https://seu-app.vercel.app
# JWT — preparado para Fase 4.1 (autenticação)
# Gerar com: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=TROCAR_EM_PRODUCAO

# --- MODELOS DE IA (Mapeamento 100% Fiel ao Código Atual) ---

# --- 1. ROTEAMENTO DE INTENÇÃO ---
# agent.py (classificar_intencao)
LLM_MODEL_ROUTING="gpt-4o-mini"
# ingestion_audio.py (_determinar_intencao_audio)
LLM_MODEL_ROUTING_AUDIO="gpt-4o"

# --- 2. EXTRAÇÃO DE CHECKLIST ---
# agent.py (extrair_checklist_silencioso)
LLM_MODEL_EXTRACTION="gpt-4o-mini"
# ingestion_audio.py (_estruturar_e_salvar_checklist) e audio_processor.py
LLM_MODEL_EXTRACTION_AUDIO="gpt-4o"

# --- 3. RAG E CONSULTAS ---
# agent.py (responder_pergunta_rag)
LLM_MODEL_RAG="gpt-4o"

# --- 4. RELATÓRIOS ---
# agent.py (gerar_relatorio_checklist_node - extração da data)
LLM_MODEL_REPORT_DATE="gpt-4o-mini"
# agent.py (gerar_relatorio_checklist_node - historinha do resumo)
LLM_MODEL_REPORT_SUMMARY="gpt-4o"

# --- 5. PERFIL VIVO ---
# profile.py (atualizar_perfil)
LLM_MODEL_PROFILE="gpt-4o"

# --- 6. DOCUMENTOS E OCR ---
# ingestion_pdf.py e pdf_processor.py
LLM_MODEL_PDF="gpt-3.5-turbo"

# --- 7. TRANSCRIÇÃO E EMBEDDINGS ---
# ingestion_audio.py e audio_processor.py
AUDIO_TRANSCRIPTION_MODEL="whisper-1"
# memory.py (buscar_contexto_documentos)
EMBEDDING_MODEL="text-embedding-3-small"

# --- 8. SCRIPTS DE MANUTENÇÃO ---
# scripts/backfill_cochilos.py
LLM_MODEL_SCRIPT_BACKFILL="gpt-4o-mini"